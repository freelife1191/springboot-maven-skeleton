package com.project.config.runner;

/**
 * Created by KMS on 08/05/2020.
 */
// @Component
// @RequiredArgsConstructor
// @EnableConfigurationProperties(PathProperties.class)
public class AppRunner { // implements ApplicationRunner {
    /*
    private final PathProperties properties;

    @Override
    public void run(ApplicationArguments args) throws Exception {
        properties.getPath().forEach(data -> System.out.println(data));
        // Arrays.asList(paths).forEach(data -> System.out.println(data));
    }
    */
}
